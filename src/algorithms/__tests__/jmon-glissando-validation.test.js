// Removed legacy glissando/pitchbend validation tests relying on deprecated fields
// (note.articulation, glissTarget). Use articulations[] and jm.audio.compileEvents
// for slide/portamento validations in new tests.
